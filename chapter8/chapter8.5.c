#include <stdio.h>

// 8.5 Пример реализации функция fopen и getc
// файловый указатель - указатель на структуру, содержащую набор данных о файле: 1) указатель на буфер
// 																				 2) счетчик символов, оставшихся в буфере
// 																				 3) указатель на очередную символьную позицию в буфере
// 																				 4) дескриптор файла
//																				 5) флаги режима чтения, записи, ошибки...

int main(void) {
	return 0;
}
